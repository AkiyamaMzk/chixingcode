二、选择题
1.在 Java 中，以下哪种方式不能获取 Class 对象？ （D）

A. Class.forName("java.lang.String") B. "Hello".getClass() C. String.class D. new String().class

2.以下哪个方法可以获取某个类的所有公有方法（包括继承的方法）？ （A）

A. getDeclaredMethods() B. getMethods() C. getPublicMethods() D. getAllMethods()

3.假设类 Person 有一个 sayHello() 方法，以下哪段反射代码可以正确调用它？B

A.

Method m = Person.class.getMethod("sayHello");
m.invoke();
B.

Method m = Person.class.getMethod("sayHello");
Person p = new Person();
m.invoke(p);
C.

Method m = Person.class.getDeclaredMethod("sayHello");
m.invoke(null);
D.

Person.class.invoke("sayHello");